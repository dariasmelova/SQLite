/* запрос демонстрирует агрегатные функции;
функция ROUND имеет аргументы x (число, которое надо округлить) и y (кол-во знаков после запятой)
*/
SELECT
SUM (Total) AS 'Сумма значений в столбце',
AVG(Total) AS 'Среднее значение в столбце',
ROUND(MAX(Total),2) AS 'Макс значение в столбце ОКРУГЛ',
ROUND(MIN(Total),2) AS 'Мин значение в столбце ОКРУГЛ',
COUNT (Total) AS 'Кол-во ненулевых записей в столбце'
FROM
invoices
