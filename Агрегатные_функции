-- запрос демонстрирует агрегатные функции
SELECT
SUM (Total) AS 'Сумма значений в столбце',
AVG(Total) AS 'Среднее значение в столбце',
MAX(Total) AS 'Макс значение в столбце',
MIN(Total) AS 'Мин значение в столбце',
COUNT (Total) AS 'Кол-во ненулевых записей в столбце'
FROM
invoices
